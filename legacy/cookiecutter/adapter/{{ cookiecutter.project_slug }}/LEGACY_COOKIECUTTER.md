# Legacy Cookiecutter Compatibility

This project is finalized by Cookiecutter's post-generation hook using the
installed SMAIRT package. The resulting files come from the same canonical
packaged assets used by `smairt new`; Cookiecutter does not maintain a second
scaffold.

For new projects, prefer `smairt new`.
